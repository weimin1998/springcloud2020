server:
  port: 80

spring:
  application:
    name: consul-consumer-order
  cloud:
    consul:
      host: 127.0.0.1
      port: 8500
      discovery:
        #hostname: localhost
        service-name: ${spring.application.name}